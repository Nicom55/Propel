Create A New Goal!

<%= form_for(@goal) do |g| %>
  <div class="field">
    <%= g.label :title %><br>
    <%= g.text_field :title %>
  </div>
  <div class="field">
    <%= g.label :body %><br>
    <%= g.text_field :body %>
  </div>
  <div class="actions">
    <%= g.submit %>
  </div>
<% end %>

<nav>
  <ul>
    <li><%= link_to "Home", root_path %></li>
  </ul>
</nav>